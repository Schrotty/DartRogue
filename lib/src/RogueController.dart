part of rogue;

class RogueController {
  RogueController() {
    /*Settings.loadSettings();
    buildStorage();

    querySelector("#inv").text = "TEST";
    armors.forEach((type, arms) => arms.forEach((id, ar) => ar.forEach((item) {
      String htmlItem = '<span> ${item.name} </span>';

      print(htmlItem);
      querySelector("#inv").appendHtml(htmlItem);
    })));*/
  }
}